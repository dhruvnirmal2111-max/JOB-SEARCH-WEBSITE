# Application Engine Run — 2026-09-07

**Status: no confirmed targets to build**

## What happened

The engine scanned `job-search/targets/shortlist.md` for rows with `Pursue? = yes`.
**Zero rows found.** No pipeline packages were built today.

## Why

All shortlist rows currently have a blank `Pursue?` column. The engine never
auto-picks unconfirmed rows — human confirmation is required before any resume,
cover letter, or outreach draft is generated.

## What to do

1. Open `job-search/targets/shortlist.md` (GitHub, the Claude mobile app, or locally).
2. Find roles you want to pursue and set their `Pursue?` column to **`yes`**.
3. The next engine run will automatically pick up those rows (up to 3 per run)
   and build the full application package (tailored resume PDF, cover letter,
   outreach drafts) plus a Google Calendar outreach reminder.

## Existing pipeline

The pipeline already has **23 company folders** from previous runs. New builds
will only happen for shortlist rows that (a) have `Pursue? = yes` **and** (b)
do not yet have a `job-search/pipeline/<company--role>/` folder.

## Salary filter

The AUD 95,000 floor (from `preferences.md`) will be applied — roles
advertised below this floor will be skipped even if marked `Pursue? = yes`.

---
*Run by: batch-apply engine (automated) · 2026-09-07*
