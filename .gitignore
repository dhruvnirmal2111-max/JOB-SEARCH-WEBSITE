# Personal documents / PDFs (resume, JDs — keep these local)
*.pdf

# Claude Code memory (project-specific, not for sharing)
.claude/projects/

# Local Claude settings (may contain credentials/permissions — keep local)
.claude/settings.local.json

# Parsed resume cache + raw extract (regenerable)
job-search/profile/base-resume.json
job-search/profile/_resume_raw.txt

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Stray build artifacts (legacy)
node_modules/
.next/
out/
tsconfig.tsbuildinfo
